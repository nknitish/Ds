let arr = [1, 3, 5, 4, 2];

function insertionSort(arr) {
  for (let i = 1; i < arr.length; i++) {
    let el = arr[i];

    for (let j = i - 1; j >= 0; j--) {}
  }
}

console.log(insertionSort(arr));
